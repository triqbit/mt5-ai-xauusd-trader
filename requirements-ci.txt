# ============================================================
# MT5 AI/ML Trading Bot — Enterprise Edition
# requirements-ci.txt  —  Linux CI-safe dependencies
#
# Excludes:
#   - MetaTrader5    (Windows-only, no Linux wheel)
#   - TA-Lib         (built from source in ci.yml)
#   - torch/torchvision/transformers/timm  (too heavy for unit tests)
#   - stable-baselines3/gymnasium          (pulls torch transitively)
# ============================================================

# -- Core numeric / data -------------------------------------------
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2
pandas==2.2.2
einops==0.7.0

# -- Technical Analysis --------------------------------------------
# TA-Lib C library is built from source in ci.yml
TA-Lib==0.4.28
pandas-ta>=0.3.14b0
mplfinance==0.12.10b0

# -- Data Acquisition ----------------------------------------------
yfinance==0.2.38
requests==2.31.0
aiohttp==3.9.5
httpx==0.27.0

# -- MT5 stub (metaapi SDK is cross-platform; MT5 binary excluded) -
metaapi-cloud-sdk==27.0.2

# -- Database ------------------------------------------------------
psycopg2-binary==2.9.9
sqlalchemy==2.0.30
alembic==1.13.1
redis==5.0.4

# -- Monitoring & Logging ------------------------------------------
prometheus-client==0.20.0
structlog==24.1.0
rich==13.7.1
tqdm==4.66.4

# -- Dashboard / API -----------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.29.0
dash==2.17.0
plotly==5.22.0

# -- Configuration -------------------------------------------------
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1
tomli==2.0.1

# -- Testing -------------------------------------------------------
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
hypothesis==6.100.1

# -- DevOps / CI (linters, formatters, security) ------------------
pre-commit==3.7.0
ruff==0.4.3
mypy==1.10.0
pip-audit==2.7.3

# -- Utilities -----------------------------------------------------
python-dateutil==2.9.0
pytz==2024.1
click==8.1.7
tabulate==0.9.0
joblib==1.4.2
