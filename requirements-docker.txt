# ============================================================
# MT5 AI/ML Trading Bot - Enterprise Edition
# requirements-docker.txt -- Linux Docker-safe dependencies
#
# Excludes:
#   - MetaTrader5 (Windows-only, no Linux wheel)
#   - metaapi-cloud-sdk (Windows-only dependency chain)
#   - pandas-ta (requires Python <3.11; runtime-only)
#   - torch/torchvision (too heavy; install separately if needed)
#   - stable-baselines3/gymnasium (pulls torch transitively)
#   - transformers/timm/einops (too heavy for base image)
# ============================================================

# -- Core numeric / data -------------------------------------------
# numpy>=2 required by TA-Lib 0.6.x
numpy>=2.4.4,<3
scipy==1.17.1
scikit-learn==1.8.0
pandas==3.0.2

# -- Technical Analysis --------------------------------------------
# Python wrapper version must be compatible with C lib 0.6.4 built in Dockerfile
TA-Lib>=0.6.8
mplfinance==0.12.10b0

# -- Data Acquisition ----------------------------------------------
yfinance==1.3.0
requests==2.33.1
aiohttp==3.13.5
httpx==0.28.1

# -- Database ------------------------------------------------------
psycopg2-binary==2.9.12
sqlalchemy==2.0.49
alembic==1.18.4
redis==7.4.0

# -- Monitoring & Logging ------------------------------------------
prometheus-client==0.25.0
structlog==25.5.0
rich==15.0.0
tqdm==4.67.3

# -- Dashboard / API -----------------------------------------------
fastapi==0.136.1
uvicorn[standard]==0.46.0
dash==4.1.0
plotly==6.7.0

# -- Configuration -------------------------------------------------
python-dotenv==1.2.2
pydantic==2.13.3
pydantic-settings==2.14.0
tomli==2.4.1

# -- Utilities -----------------------------------------------------
python-dateutil==2.9.0.post0
pytz==2026.1.post1
click==8.3.3
tabulate==0.10.0
joblib==1.5.3
