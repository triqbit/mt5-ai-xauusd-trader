# ============================================================
# MT5 AI/ML Trading Bot - Enterprise Edition
# requirements-docker.txt -- Linux Docker-safe dependencies
#
# Excludes:
#   - MetaTrader5 (Windows-only, no Linux wheel)
#   - metaapi-cloud-sdk (Windows-only dependency chain)
#   - pandas-ta (requires Python <3.11; runtime-only)
#   - torch/torchvision (too heavy; install separately if needed)
#   - stable-baselines3/gymnasium (pulls torch transitively)
#   - transformers/timm/einops (too heavy for base image)
# ============================================================

# -- Core numeric / data -------------------------------------------
# numpy>=2 required by TA-Lib 0.6.x
numpy>=2.2.4,<3
scipy==1.15.2
scikit-learn==1.6.1
pandas==2.2.3

# -- Technical Analysis --------------------------------------------
# Python wrapper version must be compatible with C lib 0.6.4 built in Dockerfile
TA-Lib>=0.6.4
mplfinance==0.12.10b0

# -- Data Acquisition ----------------------------------------------
yfinance==0.2.38
requests==2.33.1
aiohttp>=3.13.3
httpx>=0.28.1

# -- Database ------------------------------------------------------
psycopg2-binary==2.9.9
sqlalchemy==2.0.30
alembic==1.13.1
redis==5.0.4

# -- Monitoring & Logging ------------------------------------------
prometheus-client==0.20.0
structlog==24.1.0
rich==15.0.0
tqdm==4.66.4

# -- Dashboard / API -----------------------------------------------
# FastAPI >= 0.115.0 pulls starlette >= 0.40.0 (CVE-2024-47874)
fastapi>=0.115.0
uvicorn[standard]==0.29.0
dash==2.17.0
plotly==5.22.0

# -- Configuration -------------------------------------------------
python-dotenv==1.2.2
pydantic==2.11.1
pydantic-settings==2.8.1
tomli==2.0.1

# -- Utilities -----------------------------------------------------
python-dateutil==2.9.0
pytz==2026.1.post1
click==8.1.7
tabulate==0.9.0
joblib==1.5.3
# Pin wheel to address CVE-2026-24049
wheel>=0.46.2
